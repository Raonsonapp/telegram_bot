version: "3.9"

services:
  anime-bot:
    build: .
    container_name: anime-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - anime-bot-data:/app/data
    environment:
      - DB_PATH=/app/data/anime.db

volumes:
  anime-bot-data:
